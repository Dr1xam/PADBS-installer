# =======================================================
# 1. ЗАЛЕЖНОСТІ І СЕРЕДОВИЩА (НЕОБХІДНО ІГНОРУВАТИ)
# =======================================================

# Ігноруємо віртуальне середовище Python.
# Містить всі встановлені бібліотеки (великий обсяг даних).
.venv/

# Ігноруємо основний каталог залежностей Python (якщо він не .venv)
venv/

# Ігноруємо автоматично згенеровані файли, пов'язані з кешем Python
__pycache__/
*.pyc

# Ігноруємо службові файли IDE (PyCharm)
.idea/

# =======================================================
# 2. КАТАЛОГИ З КОНФІГУРАЦІЯМИ ТА ДАНИМИ
# =======================================================

# Ігноруємо каталог resources.
# Якщо він містить великі бінарні дані, згенеровані файли або файли,
# які не повинні бути у Git, його слід ігнорувати.
# Якщо ж він містить конфігураційні файли, необхідні для роботи,
# його слід видалити звідси!
# Припускаємо, що він містить згенеровані ресурси:
resources/

# Ігноруємо log-файли (часто великі та змінюються при кожному запуску)
logs/

# =======================================================
# 3. КАТАЛОГИ З КОНФІГАМИ АВТОМАТИЗАЦІЇ
# =======================================================

# Ігноруємо каталог ansible.

ansible/

# Ігноруєм скрипт завантаження
download.sh