# Proxmox Auto-Deployment: Base Services

> Автоматизований інструмент для розгортання віртуальних машин у середовищі Proxmox VE. Забезпечує повний цикл розгортання машин із встановленим програмним забезпеченням (Наданий момент реалізовано лише Rocketchat-server).

## 🛠 Технології

### Virtualization & System
- **Proxmox VE** - Цільова платформа для віртуалізації (використання CLI `qm`, `pct`).
- **Ubuntu Server** - Базова операційна система для шаблону.
- **QEMU Guest Agent** - Взаємодія хоста з віртуальною машиною.
- **Cloud-Init** - Автоматичне налаштування мережі при клонуванні.

### Scripting & Tools
- **Bash** - Основна логіка оркестрації та виконання команд.
- **Python 3** - Допоміжні скрипти (парсинг версій, HTTP-сервер для передачі файлів).
- **Aria2** - Багатопотокове завантаження великих архівів.

### Application
- **Rocket.Chat** - Цільовий сервіс для розгортання.
- **Snapd** - Менеджер пакетів, що використовується для інсталяції сервера чату.

##  Запуск проєкту

### Передумови
- Встановлений **Proxmox VE**.
- **Root-доступ** до терміналу Proxmox.
- Підключення до Інтернету (для завантаження `aria2` та образів).
- Вільне місце у сховищі `local-lvm` (потрібно для розгортання VM).

### Швидкий старт

1.  **Завантажте скрипт ініціалізації (`download.sh`) на сервер.**

2.  **Запустіть процес інсталяції:**
    ```bash
    bash -c "$(curl -fsSL  https://raw.githubusercontent.com/Dr1xam/PADBS/refs/heads/main/download.sh)"
    ```
<img width="1386" height="224" alt="Знімок екрана з 2025-12-06 12-34-38" src="https://github.com/user-attachments/assets/94871c42-7e5e-4440-ac76-5a500f5740ad" />

    **Що відбудеться автоматично:**
    1.  Встановлення `aria2` (якщо відсутня) для швидкого завантаження.
    2.  Завантаження розділеного архіву шаблону (`part_archive_*`) та скриптів . (Директорія інсталятору `/var/lib/vz/dump`).
    3.  Перевірка цілісності та склеювання бекапу (`part_archive_*`) у файл `.vma.zst`.
    4.  Запуск основного пайплайну розгортання (`src/install.sh`).
    5.  При відсутності файлів після завантаження із інтернету програма видаляє поломаний інсталятор.

## Архітектура розгортання

Процес інсталяції розбитий на модулі, які виконуються послідовно:

### 1. Генерація конфігурації (`make-vm-settings.sh`)
-   **Пошук ID:** Скрипт знаходить вільні VM ID (у діапазоні 100M+).
-   **Мережа:** Автоматично визначає шлюз (`GATEWAY`) та сканує підмережу для пошуку вільної IP-адреси.
-   **Конфіг:** Створює файл `vm.conf` із параметрами ресурсів (4 Core, 4GB RAM, 50GB Disk).

### 2. Створення шаблону (`make-template.sh`)
-   Відновлює VM із завантаженого бекапу `vzdump-qemu-*.vma.zst`.
-   Присвоює ім'я `deploy-template`.
-   Логування процесу відбувається у `src/make_template.log`.

### 3. Розгортання Rocket.Chat (`make-rocketchat.sh`)
-   **Клонування:** Створює "Linked Clone" з шаблону.
-   **Ресайз:** Розширює диск до 50 ГБ.
-   **Налаштування:** Прописує IP, Gateway та DNS через налаштування Proxmox.
-   **Інсталяція:**
    -   Підіймає тимчасовий Python HTTP-сервер на хості (порт 8888).
    -   Змушує VM завантажити та виконати скрипт інсталяції через `qm guest exec`.
    -   Встановлює `snapd` та `rocketchat-server`.
<img width="1384" height="746" alt="Знімок екрана з 2025-12-06 12-43-29" src="https://github.com/user-attachments/assets/2cc30a73-8818-432d-afce-66c93e047d4d" />

### 4. Перевірка результату розгортання
-   **Перегляд ip адреси:** Потрібно зайти в термінал машини під назвою rocketchat і вести команду:
```bash
  ip a
```
<img width="1864" height="812" alt="Знімок екрана з 2025-12-06 12-43-42" src="https://github.com/user-attachments/assets/ee280927-94d0-471b-86e9-2203b5d0e531" />

тепер перейдіть в її консоль  `>_ Console`

<img width="1870" height="846" alt="Знімок екрана з 2025-12-06 13-22-33" src="https://github.com/user-attachments/assets/ad824c9b-b7bc-499c-a035-8b7375b7400a" />

якщо машина досі щось виводить то натисніть `Enter` Після цього введіть логін та пароль
<img width="1170" height="31" alt="зображення" src="https://github.com/user-attachments/assets/591a0fd1-1306-4e02-ad83-e173b29f6c5b" />

```
login: sw
password: sw
```

<img width="1244" height="453" alt="Знімок екрана з 2025-12-06 12-47-29" src="https://github.com/user-attachments/assets/3a83da09-5bec-4035-94ec-078d9b6fc801" />

Водимо команду
```bash
  ip a
```
отримаємо приблизно такий вивід
<img width="1233" height="237" alt="Знімок екрана з 2025-12-06 12-47-40" src="https://github.com/user-attachments/assets/64c98586-7f73-4fbb-bb8c-2aafac9ab6f8" />



   Вище на скриншоті кольром відмічено де шукати ip адресу нашої машини
-   **Вхід у рокетчат:** Відкрийте нову вкладку та впишіть посилання 
```url
    ip(машини):3000 
    #наприклад 
    192.168.1.50:3000
```
<img width="1919" height="42" alt="Знімок екрана з 2025-12-06 13-02-43" src="https://github.com/user-attachments/assets/8b6a1df6-2883-4603-a679-ad6028931c77" />

  Після цього вас має завантажитися веб сторінка рокетчату
    
<img width="1918" height="1159" alt="Знімок екрана з 2025-12-06 12-49-01" src="https://github.com/user-attachments/assets/8a2b1396-01d6-4c30-9a28-d570ad4ae6d5" />

## Моніторинг та Логи

Усі операції записуються у лог-файли всередині директорії `src/`:

```bash
# Лог відновлення шаблону
tail -f src/make_template.log

# Лог налаштування параметрів VM
tail -f src/deploy_rocketchat_vm.log

# Лог виконання команд всередині VM (інсталяція чату)
tail -f src/deploy_rocketchat.log
```
## Вирішення проблем

### Скрипт завис на "Очікую повної готовності VM..."
Це означає, що хост не може зв'язатися з QEMU Agent всередині VM.

1.  Перевірте статус VM у Proxmox GUI.
2.  Переконайтеся, що сервіс `qemu-guest-agent` запущений всередині гостьової ОС (якщо це можливо).

### Помилка "Network is unreachable" або IP не знайдено
Якщо скрипт не зміг автоматично визначити шлюз:

1.  Перевірте налаштування мережі хоста.
2.  Відредагуйте файл `vm.conf` (після його генерації) або задайте `AUTO_GW` вручну в скрипті `make-vm-settings.sh`.

### Проблеми з завантаженням (Checksum error)
Якщо `download.sh` повідомляє про помилку цілісності файлів:

```bash
# Видаліть тимчасові файли та спробуйте знову
rm -rf download_buffer
rm -f *.vma.zst
./download.sh
```

## Видалення інсталятора



```bash
# Переходимо у директорію 
cd /var/lib/vz/dump
# Перевіряєм наявність інсталятора 
ls
# Вивід команди
... src vzdump-qemu-815898734-2025_11_24-17_42_12.vma.zst Rocketchat.tar.gz
# Переходимо в src
cd src
# Запускємо скрипт видалення
./delete-script.sh
```
**Увага:** *Це не видалить створені віртуальні машини в Proxmox.*

## Конфігурації машин ##
-  **Перегляд:** після розгортання конфігурації за якими створювалися машини зберігаються у файлі vm.conf
щоб його переглянути потрібно
```bash
   #1.Перейти у директорію
   cd /var/lib/vz/dump
   #2.Перевірити наявність файлів інсталятора
   ls
   # Вивід команди
   ... src vzdump-qemu-815898734-2025_11_24-17_42_12.vma.zst Rocketchat.tar.gz
   #3.Переходимо в src
   cd src
   #4.Перевіряємо наявність файла із конфігурацією машин
   ls
   #5.При наявності vm.conf відкриваємо його через редактор тексту
   nano vm.conf
   #В результаті побачимо конфігурацію розгорнутих машин такого типу
   
   VM_TARGET_STORAGE="local-lvm"
   GATEWAY="ВАШ_GATEWAY_IP"
   PROXMOX_IP="ВАШ_PROXMOX_IP"
   # Конфігурація для Шаблону 
   TEMPLATE_VM_ID="ID_ДЛЯ_ШАБЛОНУ"
   UBUNTU_BACKUP_TEMPLATE_NAME="vzdump-qemu-815898734-2025_11_24-17_42_12.vma.zst"
   MAKE_TEMPLATE_LOG_FILE="./src/make_template.log"
   # Конфігурація для машини з рокетчатом
   ROCKETCHAT_VM_ID="ID_НОВОЇ_VM"
   ROCKETCHAT_VM_HOSTNAME="rocketchat"
   ROCKETCHAT_DISK="50G"
   ROCKETCHAT_VM_RAM="4096"
   ROCKETCHAT_VM_CORES="4"
   ROCKETCHAT_VM_IP="IP_АДРЕСА_VM/24"
   ROCKETCHAT_VM_BRIDGE="vmbr0"
   ROCKETCHAT_VM_DNS="8.8.8.8"
   DEPLOY_ROCKETCHAT_VM_LOG_FILE="deploy_rocketchat_vm.log"
   ROCKETCHAT_ARCHIVE_NAME="Rocketchat.tar.gz"
   ROCKETCHAT_VM_INSTALLATION_DIR="/root/offline_install"
   DEPLOY_ROCKETCHAT_LOG_FILE="deploy_rocketchat.log"
```
- **Деталі формування:** Більшість конфігурацій прописані статично але такі конфігуарації як:
```bash
   GATEWAY
   PROXMOX_IP
   # Конфігурація для Шаблону 
   TEMPLATE_VM_ID
   # Конфігурація для машини з рокетчатом
   ROCKETCHAT_VM_ID
   ROCKETCHAT_VM_IP
```
генеруються або витягуються за алгоритмами

    GATEWAY
    PROXMOX_IP 
витягуються із PROXMOX та мережі

    TEMPLATE_VM_ID
    ROCKETCHAT_VM_ID
Випадково генеруються із діапазану 100000000-999999999 та перевіряються чи не зайняті іншими машинами

    ROCKETCHAT_VM_IP
Від GATEWAY відрізається 3 октети `192.168.1.1 --> 192.168.1.` іде підстановка 4-тої октети яка вибирається із 50 до 200
наприклад `192.168.1. + 50 = 192.168.1.50` якщо зайнта то `192.168.1. + 50 = 192.168.1.51` і так далі ... поки незнайде вільного айпі 



 

